services:
  app:
    image: ai-interview-web:latest
    container_name: ai-interview
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "3000:3000"
    env_file:
      - .env
    restart: unless-stopped
